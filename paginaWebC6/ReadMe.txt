

//criar pacoite JSON
npm init -y

//Altere o script:
"scripts": {
    "dev": "sass --watch ./assets/scss:./assets/css --style compressed --no-source-map"
  },

// instalar sass
npm i -g sass

// Iniciar o RUN DEV

npm run dev